import { createStore } from 'vuex'
const state = {}
const actions = {}
const mutations = {}
const getters = {}

const store = createStore({
  state,
  actions,
  mutations,
  getters
})
export default store
